[project]
name = "valueapifrontend"
version = "0.1.1"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Jonas Blatt", email = "jonas@blatts.de" }
]
requires-python = ">=3.13"
dependencies = [
    "click>=8.1.8",
    "streamlit>=1.44.0",
    "valueapiconnector>=0.1.0",
    "watchdog>=6.0.0",
]

[project.scripts]
valueapifrontend = "valueapifrontend:cli"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
