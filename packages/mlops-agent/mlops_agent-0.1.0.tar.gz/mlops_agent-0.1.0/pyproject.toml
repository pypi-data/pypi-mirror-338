[project]
name = "mlops-agent"
version = "0.1.0"
description = "MLOps agent"
authors = [
    { name = "VESSL AI Dev Team", email = "contact@vessl.ai" }
]
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "hyperpocket>=0.5.5",
    "vessl>=0.1.192",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
