from setuptools import setup

# setup.py primarily exists to connect setuptools with pyproject.toml
# Most configuration should remain in pyproject.toml and setup.cfg
if __name__ == "__main__":
    setup()
