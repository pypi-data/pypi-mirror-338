
[project]
name = "seffybacktest"
version = "0.2.0"
description = "A GUI-based Python backtesting and portfolio analysis tool"
authors = [
    { name="Your Name", email="your.email@example.com" }
]
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "yfinance",
    "pandas",
    "numpy",
    "matplotlib",
    "tk"
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
