[tool.poetry]
name = "yandex-delivery-sdk"
version = "0.1.4"
description = "SDK для удобной работы с API Yandex Delivery"
authors = ["baatyr <batyrhanbolotbek@gmail.com>"]
readme = "README.md"
packages = [{include = "yandex_delivery"}]

[tool.poetry.dependencies]
python = "^3.9"
requests = "^2.32.3"
tenacity = "^9.0.0"

[tool.poetry.group.dev.dependencies]
flake8 = "^6.1.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"