[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ems-provider"
version = "0.0.0"
description = "placeholder name reservation package"
authors = [{name = "GÉANT SWD", email = "swd@geant.org"}]
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.7"
