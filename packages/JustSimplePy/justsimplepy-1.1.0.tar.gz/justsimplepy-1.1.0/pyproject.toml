[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "JustSimplePy"
version = "1.1.0"
description = "A library for Easy & Simple Python codes"
authors = [{name = "Kshiraj Vij"}]
dependencies = ["psycopg2"]
