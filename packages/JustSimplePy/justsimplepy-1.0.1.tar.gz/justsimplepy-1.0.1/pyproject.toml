[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "JustSimplePy"
version = "1.0.1"
description = "A library for Easy Python codes"
authors = [{name = "Kshiraj Vij"}]
dependencies = ["psycopg2"]
