# -*- coding: utf-8 -*-
from kstpy.helpers import kstFrozenset

xpl_basis = {
       kstFrozenset({"a"}),
       kstFrozenset({"b"}),
       kstFrozenset({"a", "c"}),
       kstFrozenset({"b", "c"}),
       kstFrozenset({"a", "b", "d"})
       }

