[project]
name = "bilibili-mcp-server-full"
version = "0.1.2"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "huanyushi", email = "1174601344@qq.com" }
]
requires-python = ">=3.10"
dependencies = [
    "bilibili-api-python>=17.1.4",
    "mcp[cli]>=1.6.0",
]

[project.scripts]
bilibili-mcp-server-full = "bilibili_mcp_server_full:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
