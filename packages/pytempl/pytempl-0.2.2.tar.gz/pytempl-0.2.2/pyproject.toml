[tool.poetry]
name = "pytempl"
version = "0.2.2"
description = "DSL for writing HTML user interfaces in Python."
authors = ["Saurabh Ghanekar <ghanekarsaurabh8@gmail.com>"]
license = "BSD-2-Clause"
readme = "README.md"
homepage = "https://github.com/pytempl/pytempl"
repository = "https://github.com/pytempl/pytempl"
documentation = "https://github.com/pytempl/pytempl"
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
]
packages = [{ include = "pytempl", from = "src" }]
include = ["README.md", "LICENSE.md", "CHANGELOG.md"]

[tool.poetry.dependencies]
python = "^3.10"
pydantic = "^2.10"
typing-extensions = "^4"

[tool.poetry.group.dev.dependencies]
rich = "^13.7"
black = "^24.8"
ruff = "^0.5"
pre-commit = "^3.8"
tomli = "^2.0"
click = "^8.1"


[tool.black]
line-length = 88


[tool.ruff]
line-length = 88


[tool.ruff.lint]
select = [
    # "D",      # pydocstyle
    "E",      # pycodestyle errors
    "W",      # pycodestyle warnings
    "F",      # pyflakes
    "I",      # isort
    "B",      # flake8-bugbear
    "C4",     # flake8-comprehensions
    "UP",     # pyupgrade
    "ARG001", # unused arguments in functions
]
ignore = [
    "E501", # line too long, handled by black
    "B008", # do not perform function calls in argument defaults
    "W191", # indentation contains tabs
    "B904", # Allow raising exceptions without from e, for HTTPException
]


[tool.ruff.lint.pydocstyle]
convention = "google"


[tool.versioning]
backend = "poetry"


[tool.versioning.files]
version_variable = ["src/pytempl/__init__.py:__version__"]


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
