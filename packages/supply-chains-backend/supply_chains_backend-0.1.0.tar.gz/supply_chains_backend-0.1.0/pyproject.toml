[project]
name = "supply-chains-backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Thomas Pinder", email = "tompinder@live.co.uk" }
]
requires-python = ">=3.12"
dependencies = [
    "polars>=1.26.0",
]

[project.scripts]
supply-chains-backend = "supply_chains_backend:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
