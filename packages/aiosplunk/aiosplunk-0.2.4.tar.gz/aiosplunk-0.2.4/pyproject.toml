[project]
name = "aiosplunk"
version = "0.2.4"
description = "Async Splunk SDK"
requires-python = ">=3.11"
dependencies = [
    "httpx"
]
license = { text = "MIT" }
readme = "README.md"


[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/aiosplunk"]
