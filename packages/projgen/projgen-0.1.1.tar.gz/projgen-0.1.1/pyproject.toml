

[project]
name = "projgen"
version = "0.1.1"
description = "A CLI tool to generate project structure from a JSON file"
authors = [{ name="Avinash Kumar Prajapati", email="aviavi61106@gmail.com" }]
license = "MIT"
readme = "README.md"
requires-python = ">=3.7"

[project.scripts]
projgen = "projgen.cli:main"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
