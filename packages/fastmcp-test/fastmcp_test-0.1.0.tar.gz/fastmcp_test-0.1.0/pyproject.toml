[project]
name = "fastmcp-test"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "TerminalMan", email = "84923604+SecretiveShell@users.noreply.github.com" }
]
requires-python = ">=3.13"
dependencies = [
    "mcp>=1.6.0",
]

[project.scripts]
fastmcp-test = "fastmcp_test:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
