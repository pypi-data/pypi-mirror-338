# Workflow for continuous integration tasks triggered on push to a feature branch

name: CI - Feature branches
run-name: CI - Feature branches
on:
  workflow_dispatch:
  push:
    branches: [ "feature**" ]
      
jobs:
  open-pull-request:
    # Open a PR if the branch isn't main itself
    if: ${{ github.head_ref || github.ref_name }} != 'main'
    uses: ./.github/workflows/job-open-pull-request.yml
    with:
      target: main
