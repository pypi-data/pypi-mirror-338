[build-system]
requires = ["setuptools >= 40.6.0"]
build-backend = "setuptools.build_meta"

[project]
name = "embedl_hub"
version = "2025.3.0"
description = "The efficient edge AI platform"
readme = "README.md"
requires-python = ">=3.10"
