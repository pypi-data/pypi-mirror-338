[project]
name = "trainloop-sdk"
version = "0.1.11"
description = "A Data Collection SDK for TrainLoop"
authors = [{ name = "TrainLoop", email = "hello@trainloop.ai" }]
dependencies = ["requests>=2.32.3"]
urls = { "Homepage" = "https://github.com/trainloop/sdk" }

[build-system]
requires = ["setuptools>=42", "wheel", "build>=0.10.0", "twine"]
build-backend = "setuptools.build_meta"
