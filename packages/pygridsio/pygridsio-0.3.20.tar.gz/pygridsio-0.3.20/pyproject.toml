[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pygridsio"
version = "0.3.20"
description = "This is a utility package to read in .zmap and .asc grids to numpy or xarrays"
readme = "README.md"
authors = [
    { name = "Hen Brett", email = "hen.brett@tno.nl" }
]
requires-python = ">=3.6"
dependencies = [
    "numpy",
    "pandas",
    "pykrige",
    "xarray",
    "rioxarray",
    "netCDF4",
    "plotly",
    "geopandas",
]
license-files= ["LICENSE"]

[project.urls]
Homepage = "https://ci.tno.nl/gitlab/AGS/pygridsio.git"

[tool.setuptools]
packages.find.include = ["src.pygridsio*"]
include-package-data = true

[tool.setuptools.package-data]
pygridsio = ["**/*"]
