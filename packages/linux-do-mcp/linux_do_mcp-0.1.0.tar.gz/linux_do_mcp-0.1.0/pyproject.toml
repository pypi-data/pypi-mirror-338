[project]
name = "linux-do-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=1.6.0",
]

[tool.uv.workspace]
members = ["linux-do-mcp"]
