[project]
name = "my-gentxt-utils"
version = "0.1.3"
description = ""
authors = [
    {name = "vpakala_euc",email = "vpakala@omnissa.com"}
]
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "tiktoken",
    "numpy",
    "torch"
]

[tool.poetry]
packages = [{include = "my_utils", from = "src"}]


[tool.poetry.group.dev.dependencies]
pytest = "^8.3.5"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

#[project.scripts]
#utils = "my_utils.main:main"
