[loggers]
keys=root

[logger_root]
level=ERROR 
handlers=stdout

[handlers]
keys=stdout

[handler_stdout]
class=StreamHandler
level=NOTSET
formatter=stdout
args=(sys.stdout,)

[formatters]
keys=stdout

[formatter_stdout]
format=%(asctime)s [%(levelname)s] %(name)s: %(message)s
