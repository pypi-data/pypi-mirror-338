[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "numpy_rs"
version = "0.5.0"
description = "A simple experimental library"
requires-python = ">=3.4"
dependencies = []
license = { text="MIT" }
