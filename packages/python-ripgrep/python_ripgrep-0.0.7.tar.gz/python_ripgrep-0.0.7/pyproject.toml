[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
requires-python = ">=3.8,<4.0"
authors = [
    { name = "Josh Learn", email = "josh@exponent.run" },
]
description = "A Python wrapper for ripgrep"
name = "python-ripgrep"
version = "0.0.7"
dependencies = []

[tool.maturin]
features = ["pyo3/extension-module"]