from setuptools import setup, find_packages

setup(
    name="pandas_rs9",
    version="0.1",
    packages=find_packages(),
    author="JKBlake",
    description="A library for running different recommendation system experiments",
    python_requires='>=3.6',
)
