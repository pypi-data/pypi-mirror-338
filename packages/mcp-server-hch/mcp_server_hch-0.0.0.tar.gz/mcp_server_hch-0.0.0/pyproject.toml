[project]
name = "mcp-server-hch"
version = "0.0.0"
description = "MCP server which contains tools for Home Clean Home chatbot service."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
 "mcp>=1.3.0",
 "openpyxl>=3.1.5",
 "pandas>=2.2.3",
]
[[project.authors]]
name = "Zi Min ER"
email = "zimin@alvisual.com.sg"

[build-system]
requires = [ "hatchling",]
build-backend = "hatchling.build"

[project.scripts]
mcp-server-hch = "mcp_server_hch:main"
