[project]
name = "my-rnn"
version = "0.1.6"
description = ""
authors = [
    {name = "vpakala_euc",email = "vpakala@omnissa.com"}
]
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
]

[tool.poetry]
packages = [{include = "my_rnn", from = "src"}]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
