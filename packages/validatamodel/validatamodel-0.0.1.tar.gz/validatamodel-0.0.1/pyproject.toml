[project]
name = "validatamodel"
version = "0.0.1"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.7"
dependencies = [
    "vierror==0.0.2",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
