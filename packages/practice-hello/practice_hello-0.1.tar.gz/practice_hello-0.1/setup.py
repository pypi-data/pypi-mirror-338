from setuptools import setup, find_packages
setup(
    name='practice_hello',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # Add dependencies here
        #ex 'numpy>=1.11.1'
    ],
)