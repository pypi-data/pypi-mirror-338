// Create a response status enum.
enum Status {
    unknown = "unknown",
    accept = "accept",
    reject = "reject",
    error = "error"
}

// Export the response status enum.
export default Status;
